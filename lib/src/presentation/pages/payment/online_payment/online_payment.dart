import 'package:flutter/material.dart';

class OnlinePaymentRoute extends StatelessWidget {
  const OnlinePaymentRoute({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}